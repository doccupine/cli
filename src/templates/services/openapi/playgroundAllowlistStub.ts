// Placeholder request-execution allowlist for the API playground. Overwritten
// at build time with the union of the configured OpenAPI spec servers. The
// empty stub keeps the import path present when no spec is configured.
export const playgroundAllowlistStubTemplate = `[]\n`;
