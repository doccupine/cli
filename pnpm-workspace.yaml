allowBuilds:
  esbuild: false
  sharp: false
# Disable pnpm's minimum-release-age supply-chain gate for this repo. The gate
# blocks freshly published versions, which trips installs whenever pinned
# dependencies are newer than the cutoff. Set to 0 to install any resolved
# version without waiting out the release-age window.
minimumReleaseAge: 0
